import * as THREE from 'three';
import GUI from 'lil-gui';

export default class HauntedHouse{
  #renderer;
  #scene;
  camera;

  constructor(render){

  };
  createScene(){

  };
  disposeScene(){

  };
  #animate(){
    
  }
}